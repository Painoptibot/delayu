# Invest-KK (этап 1) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Поднять в «Делаю» подсистему `invest-kk` с учётом проектов и площадок, двумя воронками + handoff, бронью без коллизий, пакетом документов, roadmap/SLA, импортом МО и дашбордом — без живых СМЭВ/Битрикс.

**Architecture:** Гибрид: кастомный домен в `models_invest.py` + сервисы `services/invest_*.py` + views/templates/menu по образцу fuel/uzhv; org/роли/membership/модули — платформенные. `industry_template="invest"`.

**Tech Stack:** Django, pytest-django, Bootstrap templates (существующий UI kit), management command seed.

**Spec:** `docs/superpowers/specs/2026-07-24-invest-kk-design.md`

## Global Constraints

- Этап 1 не вызывает СМЭВ и Битрикс; только `external_ids` JSON и feature-flag заглушки.
- Смена воронки проекта только через `InvestHandoff`.
- На площадку не более одной активной связи `booked` или `selected`.
- Accept handoff блокируется, если обязательные пункты пакета в статусе `missing`.
- Импорт Excel не пишет данные без построчного подтверждения.
- МО видит только свою `Organization`; agency/dept — весь subsystem.
- Код подсистемы seed: `invest-kk`; шаблон: `invest`.
- Следовать паттернам `models_fuel.py`, `seed_fuel_novorossiysk.py`, `menu.py` (блок template), `views.py` redirect по `industry_template`.
- Миграция после `0068_platform_subsystem_admin_flags.py` → `0069_invest_module.py` (если 0069 уже занята — следующий свободный номер).
- Коммиты частые, по завершении каждой Task; сообщения на английском в стиле репо (`feat:`, `test:`, `docs:`).

## File map

| File | Responsibility |
|------|----------------|
| `delayu/models_invest.py` | Доменные модели invest |
| `delayu/models.py` | Import `models_invest`; choice `industry_template` |
| `delayu/migrations/0069_invest_module.py` | Schema + template choice |
| `delayu/services/invest_booking.py` | Бронь / коллизии |
| `delayu/services/invest_handoff.py` | Передача воронок |
| `delayu/services/invest_package.py` | Чек-лист / readiness |
| `delayu/services/invest_roadmap.py` | SLA / overdue helpers |
| `delayu/services/invest_import.py` | Excel parse / diff / apply |
| `delayu/services/invest_scope.py` | Queryset scope по роли/org |
| `delayu/services/invest_roles.py` | ROLE_SPECS + perm_for_role |
| `delayu/services/invest_dashboard.py` | Агрегаты дашборда |
| `delayu/views_invest.py` | HTTP views |
| `delayu/forms_invest.py` | Forms |
| `delayu/urls.py` | `/invest/...` routes |
| `delayu/menu.py` | Menu block `template: invest` |
| `delayu/views.py` | Redirect after subsystem switch |
| `delayu/views_platform_console.py` | Hub link for invest (если как uzhv/fuel) |
| `delayu/mixins.py` | Invest home redirect при необходимости |
| `delayu/admin.py` | Register invest models |
| `delayu/management/commands/seed_invest_kk.py` | Seed |
| `delayu/templates/invest/*.html` | UI |
| `delayu/tests/test_invest_*.py` | Tests |

---

### Task 1: Wire `industry_template=invest` and empty models module

**Files:**
- Create: `delayu/models_invest.py`
- Modify: `delayu/models.py` (choices + import)
- Create: `delayu/migrations/0069_invest_module.py` (via makemigrations)
- Test: `delayu/tests/test_invest_template.py`

**Interfaces:**
- Consumes: `Subsystem.industry_template`
- Produces: template value `"invest"` accepted by model

- [ ] **Step 1: Write the failing test**

```python
# delayu/tests/test_invest_template.py
import pytest
from delayu.models import Subsystem

@pytest.mark.django_db
def test_subsystem_accepts_invest_template():
    sub = Subsystem.objects.create(
        code="invest-t", name="Invest T", industry_template="invest", status="active"
    )
    assert sub.industry_template == "invest"
```

- [ ] **Step 2: Run test to verify it fails**

Run: `pytest delayu/tests/test_invest_template.py::test_subsystem_accepts_invest_template -v`  
Expected: FAIL (invalid choice) or migration missing / choice not present.

- [ ] **Step 3: Minimal implementation**

In `delayu/models.py` add to `industry_template` choices:

```python
("invest", "Инвестконтур"),
```

Create `delayu/models_invest.py`:

```python
"""Инвестконтур Кубани — доменные модели (тенант = Subsystem)."""
# models added in later tasks
```

At end of `delayu/models.py` (рядом с fuel/uzhv):

```python
from delayu.models_invest import *  # noqa: E402, F403
```

Run: `python manage.py makemigrations delayu --name invest_module`

- [ ] **Step 4: Run test to verify it passes**

Run: `pytest delayu/tests/test_invest_template.py -v`  
Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add delayu/models.py delayu/models_invest.py delayu/migrations/0069_invest_module.py delayu/tests/test_invest_template.py
git commit -m "feat: add invest industry_template for Krasnodar contour"
```

---

### Task 2: Core models — Project, Site, ProjectSite + booking invariant

**Files:**
- Modify: `delayu/models_invest.py`
- Create: `delayu/services/invest_booking.py`
- Modify: migration via makemigrations
- Test: `delayu/tests/test_invest_booking.py`

**Interfaces:**
- Produces:
  - `InvestProject`, `InvestSite`, `InvestProjectSite`
  - `book_site(*, project, site, user) -> InvestProjectSite` raises `InvestBookingError`
  - `select_site(*, project, site, user) -> InvestProjectSite`

- [ ] **Step 1: Write failing tests**

```python
# delayu/tests/test_invest_booking.py
import pytest
from django.contrib.auth import get_user_model
from delayu.models import Organization, Subsystem
from delayu.models_invest import InvestProject, InvestSite, InvestProjectSite
from delayu.services.invest_booking import InvestBookingError, book_site, select_site

User = get_user_model()

@pytest.fixture
def invest_ctx(db):
    sub = Subsystem.objects.create(code="inv-b", name="B", industry_template="invest", status="active")
    org = Organization.objects.create(subsystem=sub, code="mo1", name="МО-1")
    user = User.objects.create_user("inv_u", password="x")
    project = InvestProject.objects.create(
        subsystem=sub, code="P-1", name="Проект 1", organization=org,
        funnel=InvestProject.Funnel.ATTRACTION, stage="site_pick",
    )
    site = InvestSite.objects.create(
        subsystem=sub, cadastral_number="23:00:0000000:1", name="ЗУ-1",
        organization=org, status=InvestSite.Status.ACTUAL,
    )
    return {"sub": sub, "org": org, "user": user, "project": project, "site": site}

@pytest.mark.django_db
def test_book_site_ok(invest_ctx):
    link = book_site(project=invest_ctx["project"], site=invest_ctx["site"], user=invest_ctx["user"])
    assert link.role == InvestProjectSite.Role.BOOKED

@pytest.mark.django_db
def test_second_book_blocked(invest_ctx):
    book_site(project=invest_ctx["project"], site=invest_ctx["site"], user=invest_ctx["user"])
    p2 = InvestProject.objects.create(
        subsystem=invest_ctx["sub"], code="P-2", name="Проект 2",
        organization=invest_ctx["org"], funnel="attraction", stage="site_pick",
    )
    with pytest.raises(InvestBookingError):
        book_site(project=p2, site=invest_ctx["site"], user=invest_ctx["user"])

@pytest.mark.django_db
def test_select_requires_booked_or_promotes(invest_ctx):
    book_site(project=invest_ctx["project"], site=invest_ctx["site"], user=invest_ctx["user"])
    link = select_site(project=invest_ctx["project"], site=invest_ctx["site"], user=invest_ctx["user"])
    assert link.role == InvestProjectSite.Role.SELECTED
```

- [ ] **Step 2: Run tests — expect FAIL** (models/services missing)

Run: `pytest delayu/tests/test_invest_booking.py -v`

- [ ] **Step 3: Implement models + service**

Add to `delayu/models_invest.py` (полный файл на этом шаге):

```python
"""Инвестконтур Кубани — доменные модели (тенант = Subsystem)."""
from django.conf import settings
from django.db import models


class InvestProject(models.Model):
    class Funnel(models.TextChoices):
        ATTRACTION = "attraction", "Привлечение"
        SUPPORT = "support", "Сопровождение"

    subsystem = models.ForeignKey("Subsystem", on_delete=models.CASCADE, related_name="invest_projects")
    organization = models.ForeignKey(
        "Organization", on_delete=models.PROTECT, related_name="invest_projects",
        verbose_name="МО / территория",
    )
    code = models.CharField("Код", max_length=64)
    name = models.CharField("Наименование", max_length=255)
    investor_name = models.CharField("Инвестор", max_length=255, blank=True)
    industry = models.CharField("Отрасль", max_length=128, blank=True)
    funnel = models.CharField(max_length=16, choices=Funnel.choices, default=Funnel.ATTRACTION)
    stage = models.CharField("Стадия", max_length=32, default="lead")
    owner = models.ForeignKey(
        settings.AUTH_USER_MODEL, null=True, blank=True, on_delete=models.SET_NULL,
        related_name="invest_projects_owned",
    )
    investment_amount = models.DecimalField(max_digits=18, decimal_places=2, null=True, blank=True)
    jobs_count = models.PositiveIntegerField(null=True, blank=True)
    external_ids = models.JSONField(default=dict, blank=True)
    created_at = models.DateTimeField(auto_now_add=True)
    updated_at = models.DateTimeField(auto_now=True)

    class Meta:
        unique_together = [("subsystem", "code")]
        ordering = ["-updated_at"]

    def __str__(self):
        return f"{self.code} — {self.name}"


class InvestSite(models.Model):
    class Status(models.TextChoices):
        DRAFT = "draft", "Черновик"
        IN_REVIEW = "in_review", "На проверке"
        ACTUAL = "actual", "Актуальный"
        ARCHIVED = "archived", "Архив"

    subsystem = models.ForeignKey("Subsystem", on_delete=models.CASCADE, related_name="invest_sites")
    organization = models.ForeignKey(
        "Organization", on_delete=models.PROTECT, related_name="invest_sites",
    )
    cadastral_number = models.CharField("Кадастровый номер", max_length=64)
    name = models.CharField("Наименование", max_length=255)
    area_ha = models.DecimalField(max_digits=12, decimal_places=4, null=True, blank=True)
    land_category = models.CharField(max_length=128, blank=True)
    vri = models.CharField("ВРИ", max_length=255, blank=True)
    status = models.CharField(max_length=16, choices=Status.choices, default=Status.DRAFT)
    completeness_pct = models.PositiveSmallIntegerField(default=0)
    latitude = models.DecimalField(max_digits=9, decimal_places=6, null=True, blank=True)
    longitude = models.DecimalField(max_digits=9, decimal_places=6, null=True, blank=True)
    external_ids = models.JSONField(default=dict, blank=True)
    created_at = models.DateTimeField(auto_now_add=True)
    updated_at = models.DateTimeField(auto_now=True)

    class Meta:
        unique_together = [("subsystem", "cadastral_number")]
        ordering = ["cadastral_number"]

    def __str__(self):
        return self.cadastral_number


class InvestProjectSite(models.Model):
    class Role(models.TextChoices):
        CANDIDATE = "candidate", "Кандидат"
        PROPOSED = "proposed", "Предложен"
        BOOKED = "booked", "Забронирован"
        SELECTED = "selected", "Выбран"

    project = models.ForeignKey(InvestProject, on_delete=models.CASCADE, related_name="site_links")
    site = models.ForeignKey(InvestSite, on_delete=models.CASCADE, related_name="project_links")
    role = models.CharField(max_length=16, choices=Role.choices, default=Role.CANDIDATE)
    created_at = models.DateTimeField(auto_now_add=True)
    updated_at = models.DateTimeField(auto_now=True)

    class Meta:
        unique_together = [("project", "site")]
```

`delayu/services/invest_booking.py`:

```python
from django.db import transaction
from delayu.models_invest import InvestProjectSite

ACTIVE_ROLES = (InvestProjectSite.Role.BOOKED, InvestProjectSite.Role.SELECTED)


class InvestBookingError(Exception):
    pass


@transaction.atomic
def book_site(*, project, site, user) -> InvestProjectSite:
    conflict = (
        InvestProjectSite.objects.select_for_update()
        .filter(site=site, role__in=ACTIVE_ROLES)
        .exclude(project=project)
        .select_related("project")
        .first()
    )
    if conflict:
        raise InvestBookingError(
            f"Площадка занята проектом {conflict.project.code} ({conflict.get_role_display()})"
        )
    link, _ = InvestProjectSite.objects.update_or_create(
        project=project, site=site, defaults={"role": InvestProjectSite.Role.BOOKED}
    )
    return link


@transaction.atomic
def select_site(*, project, site, user) -> InvestProjectSite:
    book_site(project=project, site=site, user=user)
    link = InvestProjectSite.objects.get(project=project, site=site)
    # снять selected с других площадок этого проекта
    InvestProjectSite.objects.filter(project=project, role=InvestProjectSite.Role.SELECTED).exclude(
        pk=link.pk
    ).update(role=InvestProjectSite.Role.PROPOSED)
    link.role = InvestProjectSite.Role.SELECTED
    link.save(update_fields=["role", "updated_at"])
    return link
```

Run makemigrations; merge into `0069` or create `0070` if 0069 already applied for template-only.

- [ ] **Step 4: Run tests — PASS**

Run: `pytest delayu/tests/test_invest_booking.py -v`

- [ ] **Step 5: Commit**

```bash
git add delayu/models_invest.py delayu/services/invest_booking.py delayu/migrations delayu/tests/test_invest_booking.py
git commit -m "feat: add invest project/site models and booking guards"
```

---

### Task 3: Handoff service (funnel change only via handoff)

**Files:**
- Modify: `delayu/models_invest.py` — add `InvestHandoff`
- Create: `delayu/services/invest_handoff.py`
- Create: `delayu/services/invest_package.py` (stub `package_is_ready` used by handoff)
- Test: `delayu/tests/test_invest_handoff.py`

**Interfaces:**
- Produces:
  - `request_handoff(*, project, user, comment="") -> InvestHandoff`
  - `accept_handoff(*, handoff, user) -> InvestProject`
  - `return_handoff(*, handoff, user, comment) -> InvestHandoff`
  - `package_is_ready(project) -> bool` (Task 4 completes real logic; here stub True if no package / or False if required missing)

- [ ] **Step 1: Failing tests**

```python
# delayu/tests/test_invest_handoff.py
import pytest
from delayu.models_invest import InvestHandoff, InvestProject
from delayu.services.invest_handoff import (
    InvestHandoffError, accept_handoff, request_handoff, return_handoff,
)
from delayu.tests.test_invest_booking import invest_ctx  # reuse fixture pattern — copy fixture locally if import fragile

# Prefer duplicating invest_ctx fixture in this file to keep tests independent.

@pytest.mark.django_db
def test_accept_moves_to_support(invest_ctx):
    # set stage package_ready
    p = invest_ctx["project"]
    p.stage = "package_ready"
    p.save(update_fields=["stage"])
    h = request_handoff(project=p, user=invest_ctx["user"])
    accept_handoff(handoff=h, user=invest_ctx["user"])
    p.refresh_from_db()
    assert p.funnel == InvestProject.Funnel.SUPPORT
    assert p.stage == "accepted"
    h.refresh_from_db()
    assert h.status == InvestHandoff.Status.ACCEPTED

@pytest.mark.django_db
def test_direct_funnel_change_not_via_model_save_helper():
    # Document invariant: views must not expose funnel field; service is sole path.
    assert hasattr(InvestProject.Funnel, "ATTRACTION")

@pytest.mark.django_db
def test_return_keeps_attraction(invest_ctx):
    p = invest_ctx["project"]
    p.stage = "package_ready"
    p.save(update_fields=["stage"])
    h = request_handoff(project=p, user=invest_ctx["user"])
    return_handoff(handoff=h, user=invest_ctx["user"], comment="Неполный пакет")
    p.refresh_from_db()
    assert p.funnel == InvestProject.Funnel.ATTRACTION
    h.refresh_from_db()
    assert h.status == InvestHandoff.Status.RETURNED
```

Also add test that accept fails when package not ready — implement after package model in Task 4; for Task 3 use monkeypatch:

```python
@pytest.mark.django_db
def test_accept_blocked_when_package_not_ready(invest_ctx, monkeypatch):
    monkeypatch.setattr("delayu.services.invest_handoff.package_is_ready", lambda project: False)
    p = invest_ctx["project"]
    p.stage = "package_ready"
    p.save(update_fields=["stage"])
    h = request_handoff(project=p, user=invest_ctx["user"])
    with pytest.raises(InvestHandoffError):
        accept_handoff(handoff=h, user=invest_ctx["user"])
```

- [ ] **Step 2: Run — FAIL**

- [ ] **Step 3: Implement**

Models:

```python
class InvestHandoff(models.Model):
    class Status(models.TextChoices):
        REQUESTED = "requested", "Запрошена"
        ACCEPTED = "accepted", "Принята"
        RETURNED = "returned", "Возвращена"

    project = models.ForeignKey(InvestProject, on_delete=models.CASCADE, related_name="handoffs")
    status = models.CharField(max_length=16, choices=Status.choices, default=Status.REQUESTED)
    requested_by = models.ForeignKey(settings.AUTH_USER_MODEL, on_delete=models.PROTECT, related_name="+")
    decided_by = models.ForeignKey(
        settings.AUTH_USER_MODEL, null=True, blank=True, on_delete=models.SET_NULL, related_name="+"
    )
    comment = models.TextField(blank=True)
    created_at = models.DateTimeField(auto_now_add=True)
    decided_at = models.DateTimeField(null=True, blank=True)
```

`invest_package.py` stub:

```python
def package_is_ready(project) -> bool:
    """Real rules in Task 4. Default True if no package exists yet."""
    from delayu.models_invest import InvestPackage
    pkg = InvestPackage.objects.filter(project=project, is_active=True).first()
    if not pkg:
        return True
    return not pkg.items.filter(required=True, status="missing").exists()
```

`invest_handoff.py`:

```python
from django.db import transaction
from django.utils import timezone
from delayu.models_invest import InvestHandoff, InvestProject
from delayu.services.invest_package import package_is_ready

class InvestHandoffError(Exception):
    pass

@transaction.atomic
def request_handoff(*, project, user, comment=""):
    if project.funnel != InvestProject.Funnel.ATTRACTION:
        raise InvestHandoffError("Передача только из воронки привлечения")
    return InvestHandoff.objects.create(
        project=project, requested_by=user, comment=comment,
        status=InvestHandoff.Status.REQUESTED,
    )

@transaction.atomic
def accept_handoff(*, handoff, user):
    if handoff.status != InvestHandoff.Status.REQUESTED:
        raise InvestHandoffError("Решение уже принято")
    if not package_is_ready(handoff.project):
        raise InvestHandoffError("Пакет не готов: есть обязательные пункты missing")
    handoff.status = InvestHandoff.Status.ACCEPTED
    handoff.decided_by = user
    handoff.decided_at = timezone.now()
    handoff.save()
    p = handoff.project
    p.funnel = InvestProject.Funnel.SUPPORT
    p.stage = "accepted"
    p.save(update_fields=["funnel", "stage", "updated_at"])
    return p

@transaction.atomic
def return_handoff(*, handoff, user, comment):
    if handoff.status != InvestHandoff.Status.REQUESTED:
        raise InvestHandoffError("Решение уже принято")
    handoff.status = InvestHandoff.Status.RETURNED
    handoff.decided_by = user
    handoff.decided_at = timezone.now()
    handoff.comment = comment
    handoff.save()
    return handoff
```

Makemigrations.

- [ ] **Step 4: PASS tests**

- [ ] **Step 5: Commit** — `feat: add invest handoff between attraction and support`

---

### Task 4: Package + checklist readiness

**Files:**
- Modify: `delayu/models_invest.py` — `InvestPackage`, `InvestPackageItem`
- Replace stub logic in `delayu/services/invest_package.py`
- Test: `delayu/tests/test_invest_package.py`

**Interfaces:**
- Produces:
  - `ensure_package(project) -> InvestPackage` (creates from DEFAULT_CHECKLIST)
  - `set_item_status(item, status, attachment=None)`
  - `package_is_ready(project) -> bool`
  - `DEFAULT_CHECKLIST: list[tuple[code, title, required]]`

- [ ] **Step 1: Failing test**

```python
@pytest.mark.django_db
def test_package_blocks_when_required_missing(invest_ctx):
    from delayu.services.invest_package import ensure_package, package_is_ready
    pkg = ensure_package(invest_ctx["project"])
    assert package_is_ready(invest_ctx["project"]) is False
    for item in pkg.items.filter(required=True):
        item.status = "attached"
        item.save(update_fields=["status"])
    assert package_is_ready(invest_ctx["project"]) is True
```

- [ ] **Step 2: FAIL then implement models**

```python
class InvestPackage(models.Model):
    project = models.ForeignKey(InvestProject, on_delete=models.CASCADE, related_name="packages")
    is_active = models.BooleanField(default=True)
    created_at = models.DateTimeField(auto_now_add=True)

class InvestPackageItem(models.Model):
    class Status(models.TextChoices):
        MISSING = "missing", "Нет"
        PENDING = "pending", "Ожидание"
        ATTACHED = "attached", "Приложено"
        OVERDUE = "overdue", "Просрочено"

    package = models.ForeignKey(InvestPackage, on_delete=models.CASCADE, related_name="items")
    code = models.CharField(max_length=64)
    title = models.CharField(max_length=255)
    required = models.BooleanField(default=True)
    status = models.CharField(max_length=16, choices=Status.choices, default=Status.MISSING)
    # file field optional: FileField blank=True or FK to platform attachment later
    file = models.FileField(upload_to="invest/packages/", blank=True)
    due_at = models.DateTimeField(null=True, blank=True)
```

`DEFAULT_CHECKLIST` codes: `egrn`, `isogd`, `rgis`, `oiv`, `focus`, `anketa`, `measures`, `protocol`.

- [ ] **Step 3–5:** migrate, PASS, commit `feat: add invest transfer package checklist`

---

### Task 5: Roadmap / SLA overdue helpers

**Files:**
- Models: `InvestRoadmapItem`
- Service: `delayu/services/invest_roadmap.py`
- Test: `delayu/tests/test_invest_roadmap.py`

**Interfaces:**
- `seed_support_roadmap(project) -> list[InvestRoadmapItem]` called from `accept_handoff`
- `overdue_items(*, subsystem) -> QuerySet`
- `mark_overdue()` for batch status update

- [ ] **Step 1: Test**

```python
@pytest.mark.django_db
def test_overdue_items(invest_ctx):
    from django.utils import timezone
    from datetime import timedelta
    from delayu.models_invest import InvestRoadmapItem
    from delayu.services.invest_roadmap import overdue_items
    InvestRoadmapItem.objects.create(
        project=invest_ctx["project"], title="Земля", code="land",
        due_at=timezone.now() - timedelta(days=1), status="open",
    )
    qs = overdue_items(subsystem=invest_ctx["sub"])
    assert qs.count() == 1
```

- [ ] **Step 2–5:** implement; wire `seed_support_roadmap` into `accept_handoff`; commit `feat: add invest roadmap SLA items`

Default support steps: `land`, `permits`, `build`, `commission` with due offsets +14/+45/+180/+365 days.

---

### Task 6: Import batch diff/apply

**Files:**
- Models: `InvestImportBatch`, `InvestImportRow`
- Service: `delayu/services/invest_import.py`
- Test: `delayu/tests/test_invest_import.py` (use `io.BytesIO` + openpyxl or csv — **prefer CSV for YAGNI** in этапе 1: `.xlsx` if openpyxl already in deps, else CSV)

**Check deps:** if `openpyxl` in requirements use xlsx; else CSV template `code,name,cadastral_number,stage,investment_amount`.

**Interfaces:**
- `parse_mo_file(file, *, subsystem, organization) -> InvestImportBatch`
- `apply_row(row, *, user) -> InvestProject | InvestSite`
- `skip_row(row)`

Row actions: `new_project`, `changed_project`, `gap`, `new_site`, `changed_site`.

- [ ] **Step 1: Test create project via apply**

```python
@pytest.mark.django_db
def test_import_apply_creates_project(invest_ctx):
    from django.core.files.uploadedfile import SimpleUploadedFile
    from delayu.services.invest_import import parse_mo_file, apply_row
    content = b"code,name,stage\nP-IMP,Imported,lead\n"
    f = SimpleUploadedFile("mo.csv", content, content_type="text/csv")
    batch = parse_mo_file(f, subsystem=invest_ctx["sub"], organization=invest_ctx["org"])
    row = batch.rows.get()
    assert row.action == "new_project"
    obj = apply_row(row, user=invest_ctx["user"])
    assert obj.code == "P-IMP"
```

- [ ] **Step 2–5:** implement without silent overwrite (changed_* requires apply); commit `feat: add invest MO CSV import with confirm apply`

---

### Task 7: Roles + queryset scope

**Files:**
- `delayu/services/invest_roles.py`
- `delayu/services/invest_scope.py`
- Test: `delayu/tests/test_invest_scope.py`

**Interfaces:**
- `ROLE_SPECS = {invest_admin, invest_agency, invest_dept, invest_mo, invest_viewer}`
- `perm_for_role(role_code, module_code) -> dict`
- `projects_for_membership(membership) -> QuerySet[InvestProject]`
- `sites_for_membership(membership) -> QuerySet[InvestSite]`

Logic:
- `invest_mo` → filter `organization=membership.organization`
- others with invest roles → all in `membership.subsystem`
- unknown → none

Module codes for seed: reuse `M02,M03,M15,M22` (+ others as needed like fuel).

- [ ] Tests for mo vs dept visibility; commit `feat: add invest roles and org scope filters`

---

### Task 8: Views — mixin, hub, projects CRUD list/detail

**Files:**
- `delayu/views_invest.py`
- `delayu/forms_invest.py`
- `delayu/templates/invest/hub.html`, `projects_list.html`, `project_detail.html`, `project_form.html`
- Modify: `delayu/urls.py`
- Test: `delayu/tests/test_invest_views.py` (login + client get 200)

**Interfaces:**
- URL names: `invest-hub`, `invest-projects`, `invest-project-detail`, `invest-project-create`, `invest-project-edit`
- Mixin `InvestSubsystemMixin` — requires `industry_template=="invest"` active membership (copy pattern from `views_uzhv.py`)

Do **not** put `funnel` on the create/edit form (read-only display). Stage editable within current funnel via allowed transitions map in form clean.

- [ ] Client tests: agency user sees create; viewer gets 403 on POST create if using ModulePermissionMixin.
- [ ] Commit `feat: add invest hub and project screens`

---

### Task 9: Sites UI + booking actions

**Files:**
- Templates: `sites_list.html`, `site_detail.html`, `site_form.html`
- Views: list/detail/create + POST `invest-site-book`, `invest-site-select`
- URLs as above
- Test booking conflict returns error message on page

- [ ] Commit `feat: add invest sites UI with booking actions`

---

### Task 10: Handoff + package UI

**Files:**
- Templates: `handoffs_list.html`, `package_detail.html`
- Views: list handoffs; request/accept/return POST; package detail + item status update + file upload
- Gate accept with `InvestHandoffError` → messages.error

- [ ] Commit `feat: add invest handoff and package UI`

---

### Task 11: Import UI

**Files:**
- `templates/invest/import_list.html`, `import_detail.html`
- Views: upload → batch detail → apply/skip row POSTs
- Only `invest_mo` / `invest_dept` / `invest_admin` (check role code on membership)

- [ ] Commit `feat: add invest MO import UI`

---

### Task 12: Dashboard

**Files:**
- `delayu/services/invest_dashboard.py` — `build_dashboard(subsystem) -> dict`
- `templates/invest/dashboard.html`
- View `InvestDashboardView`
- Test: after overdue item, dashboard dict has `overdue_count >= 1`

Dashboard keys: `attraction_counts`, `support_counts`, `overdue_count`, `packages_ready_pct`, `active_bookings`, `bottlenecks_by_org` (top orgs by overdue).

- [ ] Commit `feat: add invest leadership dashboard`

---

### Task 13: Menu, switcher redirect, console, admin

**Files:**
- `delayu/menu.py` — block `{"template": "invest", "items": [...]}`
- `delayu/templates/partials/subsystem_switcher.html` — label for invest
- `delayu/views.py` — redirect `invest` → `invest-hub`
- `delayu/views_auth_hub.py` / `views_platform_console.py` — same pattern as uzhv/fuel if present
- `delayu/mixins.py` — if uzhv special-case home exists, add invest
- `delayu/admin.py` — register models

Menu items (url_name → label): hub, projects, sites, handoffs, packages (or via project), import, dashboard.

- [ ] Test: membership switch to invest redirects to hub (extend existing switch test or new).
- [ ] Commit `feat: wire invest menu and subsystem entrypoints`

---

### Task 14: Seed command

**Files:**
- `delayu/management/commands/seed_invest_kk.py`

Seed:
1. `call_command("seed_catalog")`
2. Subsystem `invest-kk`, template `invest`, status active, color `#0f766e`
3. Orgs: `dept` (Департамент), `agency` (Агентство), `mo-krasnodar`, `mo-sochi`
4. Roles from `ROLE_SPECS`; permissions via `perm_for_role`
5. Users: `invest_admin`, `invest_agency`, `invest_dept`, `invest_mo`, `invest_viewer` (password = username for local demo)
6. Demo: 3 projects (2 attraction, 1 support), 4 sites, one booked link, one handoff requested, packages, roadmap on support project
7. Enable modules M02,M03,M15,M22 (+ license entitlements like uzhv seed)

- [ ] Manual: `python manage.py seed_invest_kk` then login as `invest_agency`
- [ ] Commit `feat: add seed_invest_kk demo contour`

---

### Task 15: End-to-end scenario tests

**Files:**
- `delayu/tests/test_invest_e2e.py`

Scenarios:
1. Agency books site → completes package → requests handoff → dept accepts → funnel support + roadmap exists.
2. MO user cannot see other MO project.
3. Second book on same site fails.
4. Dashboard overdue after past-due roadmap item.

- [ ] Run: `pytest delayu/tests/test_invest_*.py -v` — all PASS
- [ ] Commit `test: cover invest-kk stage-1 end-to-end scenarios`

---

## Self-review (plan vs spec)

| Spec section | Task(s) |
|--------------|---------|
| Architecture / template invest | 1, 13, 14 |
| InvestProject / Site / ProjectSite / booking | 2, 9 |
| Dual funnel + Handoff | 3, 10 |
| Package checklist | 4, 10 |
| Roadmap/SLA | 5, 12 |
| Import MO | 6, 11 |
| Dashboard | 12 |
| Roles + MO scope | 7, 14, 15 |
| No SMEV/Bitrix in stage 1 | Global + external_ids only in models |
| Seed + criteria готовности | 14, 15 |

No TBD placeholders left. Names consistent: `book_site`, `package_is_ready`, `request_handoff`, `accept_handoff`, `projects_for_membership`.

---

## Execution handoff

Plan saved to `docs/superpowers/plans/2026-07-24-invest-kk.md`.

**Two execution options:**

1. **Subagent-Driven (recommended)** — fresh subagent per task, review between tasks  
2. **Inline Execution** — this session with executing-plans and checkpoints  

Which approach?
